# Optional: Gemini / legacy client-side keys (only if you wire the app to call Gemini from the browser)
# GEMINI_API_KEY=

# Point the UI at your FastAPI server (enables live API instead of in-browser mocks)
# VITE_API_BASE_URL=http://127.0.0.1:8000
